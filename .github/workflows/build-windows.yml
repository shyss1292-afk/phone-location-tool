name: Build Windows EXE

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt

      - run: pyinstaller --noconfirm --onefile --windowed --add-data "prefix_db.json;." phone_tool.py

      - uses: actions/upload-artifact@v4
        with:
          name: phone-tool-windows
          path: dist/phone_tool.exe
