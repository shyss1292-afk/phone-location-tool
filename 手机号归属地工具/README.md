# 手机号批量归属地识别工具
双击运行打包后的 `phone_tool.exe`。导入 xlsx 后自动识别手机号列，点击“识别”并可导出。程序全程本地处理，不上传手机号。号段数据库为 `prefix_db.json`，可直接扩展 prefixes；当前文件明确标注更新时间与覆盖范围。Windows 打包：在 Windows 中运行 `build_windows.bat`，生成 `dist/phone_tool.exe`。
